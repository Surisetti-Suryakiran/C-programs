#include<stdio.h>
main()
{
	int n,m,sum=0;
	scanf("%d",&n);
	while(n>0)
	{
		m=n%10;
		sum=sum+m;
		n=n/10;
	}
    n=sum;
    sum=0;
    while(n>0)
	{
		m=n%10;
		sum=sum+m;
		n=n/10;
	}
	printf("\n%d",sum);
}
